public static class Enums
{
    public enum ScreenView
    {
        LoadScreen,
        MainMenu,
        PlayModeScreen,
        StoreScreen,
        Setting
    }

    public enum Scene
    {
        MainScene,
        PlayMode
    }

    public enum WeaponType
    {
        Sword,
        Axe,
        Bow
    }
}
